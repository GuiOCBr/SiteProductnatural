@model ProductListViewModel

<div class="text-center">
    <h2>@Model.CurrentCategory</h2><br />
</div>

<div class="container">
    <div class="row">
        @foreach(var product in Model.Products)
        {
            <partial name="_ProductSummary" for="@product"/>
        }
    </div>
</div>
